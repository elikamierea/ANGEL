# LOD and visibility

The system decides how much detail to display based on the on-screen area of each node:

- `hidden`: too small to draw
- `simplified` / `name` / `detail`: progressively more information becomes visible

Note: box selection already supports including hidden nodes, to avoid missing them during batch operations in large projects.

- Back to directory: (Back to Home)[home.md]
