# LOD と可視性

システムは各ノードの画面上の面積に応じて、表示する詳細度を決定します：

- `hidden`：小さすぎて描画しない
- `simplified` / `name` / `detail`：段階的に表示される情報が増える

注意：範囲選択は hidden ノードを含めることに対応済みで、大規模プロジェクトでの一括操作時に選択漏れを防ぎます。

- 目次に戻る： (Back to Home)[home.md]
