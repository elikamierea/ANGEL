# LOD와 가시성

시스템은 각 노드가 화면에서 차지하는 면적에 따라 표시할 세부 수준을 결정합니다:

- `hidden`：너무 작아 그리지 않음
- `simplified` / `name` / `detail`：점진적으로 더 많은 정보가 표시됨

참고: 범위 선택은 hidden 노드 포함을 이미 지원하므로, 대규모 프로젝트에서 일괄 작업 시 선택 누락을 방지합니다.

- 목차로 돌아가기： (Back to Home)[home.md]
